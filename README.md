# Travel Diary MVP

## 실행 방법

브라우저에서 `index.html`을 직접 열면 됩니다.

실제 위치 기록은 브라우저 정책상 `localhost` 또는 HTTPS 환경에서 더 안정적으로 동작합니다.

## 첫 화면 주소

`file:///C:/Users/user/Desktop/travel%20diary/index.html`

## 흐름

`여행 시작 -> 여행 지도 -> 기록 시작 -> 기록 종료 -> 다이어리 탭`
